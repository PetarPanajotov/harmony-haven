import { PopularFilterPipe } from './popular-filter.pipe';

describe('PopularFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PopularFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
